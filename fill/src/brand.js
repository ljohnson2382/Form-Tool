// Placeholder brand for the respondent-facing app — swap for your project's
// actual brand config (same shape the admin app uses; see
// demo/src/brands/staging.js for an example).
export const fillBrand = {
  appName: 'Forms',
}
