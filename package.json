{
  "name": "form-builder-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:kit": "npm run build --workspace packages/form-builder-kit",
    "dev": "npm run build:kit && npm run dev --workspace apps/demo",
    "build": "npm run build:kit && npm run build --workspace apps/demo"
  }
}
